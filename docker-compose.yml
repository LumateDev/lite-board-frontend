version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    volumes:
      - ./backend/media:/usr/share/nginx/html/media  # Монтирование медиа файлов для фронта

networks:
  default:
    driver: bridge 